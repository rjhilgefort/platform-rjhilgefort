---
"@repo/openclaw-voice-bridge": minor
---

Add thinking indicator: soft tapping sound loops while the bot is processing (LLM calls, tool calls). Stops automatically when the first TTS audio starts playing. Configurable via THINKING_INDICATOR_ENABLED env var.
